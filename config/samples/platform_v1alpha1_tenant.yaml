apiVersion: platform.platform.io/v1alpha1
kind: Tenant
metadata:
  labels:
    app.kubernetes.io/name: custom-tenant-operator
    app.kubernetes.io/managed-by: kustomize
  name: tenant-sample
spec:
  # TODO(user): Add fields here
